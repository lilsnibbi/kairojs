/**
 * Hot module replacement for pieces, available as `kairojs/hmr`.
 *
 * Register {@link HmrPlugin} to have it follow the client's lifecycle, or call {@link start}
 * yourself once pieces are loaded if you would rather control when watching begins.
 *
 * @since 1.0.0
 */

export * from "./lib/hmr.ts";
export * from "./lib/plugin.ts";
